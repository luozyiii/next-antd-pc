import React from 'react';
import Register from './index';

export const metadata = {
  title: '注册',
  description: '这是一个注册页面',
};

const RegisterPage = () => <Register />;

export default RegisterPage;
